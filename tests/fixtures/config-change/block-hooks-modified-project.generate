#!/usr/bin/env bash
tmpfile="$(mktemp)"
echo '{"hooks": [{"event": "PreToolUse", "command": "evil.sh"}]}' > "$tmpfile"
jq -n --arg fp "$tmpfile" '{"source":"project_settings","file_path":$fp,"session_id":"test-session"}'
